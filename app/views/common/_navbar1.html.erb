<nav class="navbar navbar-default" role="navigation">
    <div class="container-fluid nav1">
        <div class="navbar-header">
            <a class="navbar-brand" href="/"><i class="fa fa-diamond" aria-hidden="true">真·24K Offer</i></a>
        </div>


        <div class="collapse navbar-collapse">
            <!-- <ul class="nav navbar-nav">
              <li>
                <%= link_to("应征职位", jobs_path) %>
              </li>
              <li>
                <div class="nav navbar-nav navbar-left">
                  <%= form_tag search_jobs_path , :method => :get do %>
                  <div class="input-group" id="search-id">
                    <input type="text" class="form-control" name="q" value="<%= params[:q] %>" placeholder="请输入职位信息">
                    <div class="input-group-btn">
                      <button class="btn btn-default" type="submit">
                        <i class="glyphicon glyphicon-search"></i>
                      </button>
                    </div>
                  </div>
                  <% end %>
                </div>
              </li>
            </ul> -->
            <ul class="nav navbar-nav navbar-right">
                <% if !current_user %>
                <!-- <li><a href="#" data-toggle="modal" data-target="#signup-modal"><i class="fa fa-user-plus" aria-hidden="true"></i> 注册</a></li>
                <li><a href="#" data-toggle="modal" data-target="#login-modal"><span class='glyphicon glyphicon-log-in'></span> 登录</a></li> -->
                <li>
							    <%= link_to("<span class='glyphicon glyphicon-plus-sign'></span>注册".html_safe, new_user_registration_path) %>
						    </li>
						    <li>
					    	  <%= link_to("<span class='glyphicon glyphicon-log-in'></span>登录".html_safe, new_user_session_path) %>
						    </li>
                <% else %>
                  <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                        <i class="fa fa-user-circle-o" aria-hidden="true"></i> <%= current_user.email %>
                        <b class="caret"></b>
                    </a>
                    <ul class="dropdown-menu">
                        <% if current_user.admin? %>
                        <li> <%= link_to("<span class='glyphicon glyphicon-user'></span> 管理员".html_safe, admin_jobs_path) %> </li>
                        <% end %>
                        <li> <%= link_to("<span class='glyphicon glyphicon-log-out'></span> 退出".html_safe, destroy_user_session_path, method: :delete) %> </li>
                    </ul>
                  </li>
                <% end %>
            </ul>
        </div>
    </div>
</nav>
